# Next5h - macOS 原生 Codex 5 小时自动续航与任务调度工作台方案

## 项目概述 (Overview)

针对 GPT Plus 用户使用本地 Codex 客户端（ChatGPT.app，Bundle ID: `com.openai.codex`）时的 **5 小时（300 分钟）滑动窗口用量限制**，打造一款 **macOS 原生独立桌面应用（Next5h）**。
该应用在本地静默监测 5 小时额度重置时间，提供直观的任务编排工作台，允许用户在限流期间预先编写 Prompt，指定“项目 -> 会话”、选择目标模型（Model）与思考强度（Reasoning Effort），并在额度解封后（默认 +1 分钟安全缓冲），自动完成 Mac 休眠唤醒与静默/前台精准发送。

应用在**首次安装启动时，默认自带一条预置配置**（每天早上 06:00、轻量模型、最低思考强度、向常规对话发送“嗨”），用户开箱即用，也可随时修改。

---

## 核心系统架构与模块设计 (Architecture)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    Next5h macOS 独立桌面工作台 (本地联动版)                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                               UI 展现层 (SwiftUI)                           │
│   ┌──────────────────────┬───────────────────────────┬──────────────────┐   │
│   │   左侧边栏 (Sidebar)  │    中间主工作区 (Composer) │   右侧状态面板   │   │
│   │ • 5H 状态 & 解锁时间  │ • 目标路由 (先选项目->会话)│ • 5H 额度柱状图  │   │
│   │ • 待发队列 & 历史记录 │ • 🤖 模型与思考强度选择器  │ • 解锁倒计时     │   │
│   │ • 项目导航           │ • Prompt 编辑器 (Markdown)│ • 本地客户端状态 │   │
│   │                      │ • 发送模式 & +1min 缓冲开关│ • 权限健康状态   │   │
│   └──────────────────────┴───────────────────────────┴──────────────────┘   │
├─────────────────────────────────────────────────────────────────────────────┤
│                              核心业务逻辑层 (Engine)                         │
│   ┌─────────────────────┐  ┌──────────────────────┐  ┌──────────────────┐   │
│   │  QuotaProbeEngine   │  │   JobQueueManager    │  │  SessionRouter   │   │
│   │  • 本地低频节能探针  │  │ • 任务生命周期管理   │  │ • 本地项目/会话路由│   │
│   │  • 捕获提前 Reset   │  │ • 默认初始任务预置   │  │ • 模型与思考参数绑定│   │
│   └─────────────────────┘  └──────────────────────┘  └──────────────────┘   │
├─────────────────────────────────────────────────────────────────────────────┤
│                           系统级保障与调度层 (OS Guardians)                 │
│   ┌─────────────────────┐  ┌──────────────────────┐  ┌──────────────────┐   │
│   │    PowerGuardian    │  │  NetworkReachability │  │ SmartScheduler   │   │
│   │ • IOPMSchedulePower │  │ • NWPathMonitor      │  │ • 默认 +1min 缓冲 │   │
│   │ • IOPMAssertion 锁定│  │ • 唤醒后 Wi-Fi 缓冲  │  │ • 简单容错二次确认│   │
│   └─────────────────────┘  └──────────────────────┘  └──────────────────┘   │
├─────────────────────────────────────────────────────────────────────────────┤
│                              发送执行层 (Dispatchers)                       │
│       ┌───────────────────────────────┬───────────────────────────────┐     │
│       │  SilentAPIDispatcher (静默后台)│ ForegroundUIDispatcher (前台)  │     │
│       │  • 携带 Model + Effort 直接提交│  • 激活本地 Codex 窗口并模拟   │     │
│       └───────────────────────────────┴───────────────────────────────┘     │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 核心功能与技术细节设计

### 1. 开箱即用的“默认初始任务配置”（Default Initial Preset）
用户初次安装打开应用时，无需面对空白页面，系统默认预填好一组合理的日常任务配置：
*   **触发时间**：每天早上 `06:00`；
*   **目标会话**：无项目（常规独立对话）；
*   **模型配置**：最经济轻量的模型（如 `gpt-4o-mini` 或 `o3-mini`），思考强度设为最低（`Low`）；
*   **Prompt 内容**：`"嗨"`；
*   **状态**：用户可一键点击“排定启用”，也可根据个人习惯自由调整内容和时间。

### 2. 模型选择（Model）与思考强度（Reasoning Effort）
*   **模型选择器**：支持 `Codex`、`o3-mini`、`o1`、`GPT-4o`、`GPT-4o-mini`；
*   **思考强度分段控制器**（仅在推理模型下激活）：
    *   ⚡️ **低 (Low)**：快速响应，适合日常小改动与轻量任务；
    *   ⚖️ **中 (Medium)**：标准默认，兼顾推理质量与速度；
    *   🔬 **高 (High)**：深度严密推理，适合复杂架构与深层 Bug 排查。

### 3. 本地低频探针与 5 小时额度监测 (QuotaProbeEngine)
*   **读取本地凭据**：直接读取本地运行的 Codex 客户端（`~/Library/Application Support/Codex/` 与 Keychain）的 Session 会话；
*   **低频节能轮询**：
    *   `距离重置 > 1 小时`：每 30 ~ 60 分钟极轻量同步一次（或切回 App 时主动刷新）；
    *   `距离重置临近 (10m ~ 1h)`：每 10 ~ 15 分钟同步一次；
    *   `到达重置时间`：在预定触发前单次确认。
*   **捕获提前 Reset**：检测到 `usedPercent < 100%` 自动提前唤醒。

### 4. 直观的“项目 -> 会话”两步联动路由 (SessionRouter)
1.  **第一步：选择项目范围（Project Scope）**
    *   `选择具体项目`（从本地读取的 Projects 列表中选择，如 `Next5h-Core`）
    *   `无项目`（全局通用对话）
2.  **第二步：选择会话形式（Conversation Action）**
    *   若选了具体项目：① 在该项目内新建对话；② 选择该项目现存的某条历史会话追加。
    *   若选了无项目：① 新建独立通用对话；② 选择独立历史会话追加。

### 5. +1 分钟安全缓冲延时 (Safety Delay Buffer)
*   **默认触发时间**：`resets_at + 1 分钟`，主动慢 1 分钟彻底规避本地/服务端时钟差及 429 竞争；
*   **轻量兜底**：若偶发网络异常，等待 30 秒自动重试 1 次。

### 6. 发送模式与本地客户端联动 (Dispatchers)
*   **静默后台发送（Silent API）**：携带选定的 `model` 和 `reasoning_effort` 直接向后端接口提交，不弹窗、不抢焦点，本地 Codex 客户端通过自身数据流无缝同步最新消息；
*   **呼出窗口发送（Foreground UI）**：使用 `NSRunningApplication` 激活本地 `/Applications/ChatGPT.app` 窗口，定位输入框并模拟发送。

### 7. Mac 睡眠唤醒与网络保障 (PowerGuardian & NetworkMonitor)
*   `IOPMSchedulePowerEvent`：硬件 RTC 定时唤醒休眠/合盖的 Mac（支持 Dark Wake 暗唤醒）；
*   `IOPMAssertionCreateWithName`：执行期锁定系统电源防止中途休眠；
*   `NWPathMonitor`：唤醒后等待 2~5 秒 Wi-Fi 就绪后再发请求；
*   `UNUserNotificationCenter`：发送完成推送通知，点击一键拉起本地 Codex 客户端直达会话。

---

## 落地实施阶段 (Implementation Phases)

| 阶段 | 核心任务 | 输出物 |
| :--- | :--- | :--- |
| **Phase 1** | **工程与基础数据模型** | 初始化 Swift / SwiftUI 工程，建立 `ScheduledJob`（含模型、思考强度、默认初始预置）、`QuotaSnapshot` |
| **Phase 2** | **低频探针与缓冲调度器** | 实现 `QuotaProbeEngine`、`PowerGuardian`（RTC唤醒/防休眠）、`NetworkMonitor` 与 `SmartScheduler` |
| **Phase 3** | **会话路由与发送执行器** | 实现 `SessionRouter`、`SilentAPIDispatcher`（携带模型与思考参数）与 `ForegroundUIDispatcher` |
| **Phase 4** | **SwiftUI 原生三栏工作台** | 构建主界面、默认初始配置展示、模型与思考强度组件、项目/会话二级选择器、5H 额度状态栏 |
| **Phase 5** | **通知与权限引导集成测试** | 完善通知一键跳转、权限检查向导，进行完整功能联调与验证 |

---

## 验证与测试计划 (Verification Plan)

1.  **初始配置预填验证**：验证首次启动 App 时，编辑器默认正确填入“06:00、常规会话、轻量模型、思考强度 Low、内容‘嗨’”；
2.  **模型与思考强度验证**：测试选择 `o3-mini (High)`、`codex (Medium)`、`gpt-4o` 时，参数是否正确绑定并随请求正确派发；
3.  **路由联动验证**：测试选择“具体项目 vs 无项目”后，二级“新建 vs 追加历史会话”的正确过滤和绑定；
4.  **+1min 缓冲与调度测试**：验证在 `resets_at + 60s` 时准时触发，以及手动指定时间模式；
5.  **睡眠唤醒与 Wi-Fi 恢复测试**：验证合盖休眠后 RTC 准时唤醒，并等待网络就绪后再发起发送。
