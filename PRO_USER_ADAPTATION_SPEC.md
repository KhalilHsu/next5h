# Next5h - ChatGPT Pro 用户全场景适配与体验设计规范 (Pro User Adaptation Spec)

> **文档定位**：针对 ChatGPT Pro（$200/月）订阅用户取消 5 小时消息频次限制（无限/高上限畅用）的特性，定义 Next5h 状态栏视觉呈现、应用调度能力演进与底层技术实现规范。

---

## 📌 背景与核心定位演进

| 维度 | Plus 用户（常规模式） | Pro 用户（尊享/流水线模式） |
| :--- | :--- | :--- |
| **主要限制** | 5 小时滑动窗口（300 分钟消息频次限制） + 7 天周限额 | **无 5H 限制**（标准/常规模型无限畅用），主要受 7 天周限额与 o1-pro 深度推理算力池约束 |
| **核心痛点** | 频繁被 5H 窗口限流打断工作流，额度到期浪费 | 算力充裕但缺乏自动化排定手段，多任务/长任务需手动重复触发 |
| **应用定位** | **「额度救火与算力续航」**（被 5H 锁住后卡点自动唤醒派发） | **「无人值守自动化流水线与异步调度中心」**（定时批量、流水线连续派发、深夜离线自动编码） |

---

## 一、 状态栏（Status Bar）差异化展示设计

在保持 **28pt 极窄单图标占用** 的 macOS 原生设计规范下，针对 Pro 用户提供专属的自适应视觉与菜单交互。

### 1. 28pt 极窄微胶囊视觉对比

```
┌────────────────────────────────────────────────────────┐
│  Plus 模式 (双额度胶囊)         │   Pro 模式 (尊享/状态胶囊)      │
│         ┌───────────┐         │            ┌───────────┐           │
│  上层:   │  5H: 82%  │ (5H剩余) │   上层:    │    PRO    │ (或 ∞ 标识)│
│  下层:   │ 周: 65%   │ (周剩余) │   下层:    │  周: 85%  │ (周/深度额度)│
│         └───────────┘         │            └───────────┘           │
│        (宽度保持 28pt)         │           (宽度保持 28pt)           │
└────────────────────────────────────────────────────────┘
```

* **上层胶囊（身份与免限标识）**：
  * 显示 **`PRO`** 或 **`∞`** 标识（黑色胶囊底色 + 白色高对比度等宽粗体），清晰传达当前免受 5H 频次限制。
* **下层胶囊（高级算力/周额度监控）**：
  * 显示 **周额度或 o1-pro 深度思考剩余百分比**（如 `85`），保障用户对每周高阶算力池的掌控。
* **动态任务执行指示（运行态）**：
  * 当后台正在通过 CLI 静默派发任务或执行队列时，下层胶囊可动态切换为闪烁的 `⚡️` 动画或任务进度指示（如 `1/3`）。

### 2. 状态栏下拉菜单 (NSMenu) 结构重构

点击顶部状态栏图标呼出的菜单层级与文案自适应调整：

1. **顶部工作台入口**：`🖥️ 打开 Next5h 工作台 (⌘O)`
2. **用户身份与特权状态**：
   * `👑 ChatGPT Pro 专属权益 · 5H 限制已解除 (Unlimited)`
3. **分层额度明细**：
   * `🟢 标准/高级模型：无限制 (随时可用)`
   * `🔵 每周深度推理额度：剩余 85% · (重置于 周五 14:00)`
4. **当前调度队列概览**：
   * `📋 排队中任务：3 个 · 下次唤醒派发：明天 07:00`
5. **快捷操作**：
   * `🔄 立即刷新状态 (⌘R)`
   * `退出 Next5h (⌘Q)`

---

## 二、 整体应用的使用场景与功能演进 (Application Usage)

Pro 用户不再需要“等待 5H 窗口解封”，Next5h 的功能矩阵全面转向自动化生产力释放：

### 1. 任务调度策略（Schedule Strategy）智能适配

* **智能隐藏 / 提示「5H 解封后自动发送」**：
  * 在任务编排器中，检测到 Pro 账号后，将「5H 额度解封自动发送」选项自动置灰，并附带提示：*“Pro 用户无 5H 窗口限制，建议使用定时派发或流水线模式”*。
* **强化 Pro 专属核心调度场景**：
  1. **深夜无人值守自动化流水线（Daily / Scheduled Pipelines）**：
     * 开发者睡前将 5 个复杂模块的重构或单元测试编写任务排定在「凌晨 03:00」；
     * Next5h 准时通过 RTC 硬件唤醒 Mac，在后台静默依次调用本地 Codex CLI 执行，早晨开盖即可直接检视代码 Diff 与 Git 提交。
  2. **任务链与连续派发（Task Chaining / FIFO Pipeline）**：
     * 无需等待 5H 冷却，支持「前序任务执行完成（或成功退出）后，自动触发下一个排定任务」，实现多会话、多项目的连贯执行。
  3. **基于每周高阶算力池的攻坚排定（Weekly Reset Strategy）**：
     * 针对消耗极大的 o1-pro 架构级重构任务，排定在每周算力池重置第一时间集中计算执行。

### 2. 监控看板（Dashboard）的自适应布局

* **主监控卡片**：从「5 小时滑动周期窗口」自动演进为「**PRO 尊享全速通道**」，呈现“免等待、全并发、随时就绪”的绿色健康状态。
* **副监控卡片**：聚焦展示「**7 天每周额度 / 深度推理算力池**」的时间与百分比追踪。
* **新增生产力效能统计**：展示“本周已自动派发会话数”、“夜间无人值守执行次数”、“累计节省等待时间”。

---

## 三、 技术层面的实现落地路径 (Technical Architecture)

现有代码架构高度模块化，Pro 模式的支持可直接在现有数据链路中平滑扩展：

### 1. 数据模型扩展 (`QuotaSnapshot.swift` & `LiveQuotaResponse`)

```swift
public struct QuotaSnapshot: Codable, Equatable {
    public var usedPercent: Double
    public var resetsAt: Date?
    public var windowMinutes: Int
    public var weeklyUsedPercent: Double?
    public var weeklyResetsAt: Date?
    public var planType: String           // "pro" | "plus" | "free" | "team"
    public var isProUser: Bool { planType.lowercased() == "pro" }
    // ...
}
```

### 2. 上下文与探针层 (`LocalCodexContextReader.swift` & `QuotaProbeEngine.swift`)

* 从 `~/.codex/auth.json` 或 `backend-api/wham/usage` 响应体中直接提取 `plan_type`；
* 当 `plan_type == "pro"` 时，若官方接口未返回 `primary_window` 或返回无限制，自动将 `QuotaSnapshot` 标识为 Pro 模式。

### 3. 状态栏渲染器 (`StatusItemRenderer.swift`)

* 在 `renderDualCylinder` 中增加 `isProUser: Bool` 分支：
  * Pro 模式下，上层固定绘制 `PRO` 徽标或 `∞` 符号，下层保持绘制周额度或任务排队状态。

### 4. 任务编排与调度视图 (`StrategyPickerView.swift` & `SmartScheduler.swift`)

* 策略选择器根据 `QuotaProbeEngine.shared.currentQuota.isProUser` 动态展示/置灰 5H 策略，默认推荐「指定时间」与「定时循环」策略。
